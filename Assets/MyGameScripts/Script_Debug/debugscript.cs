using UnityEngine;

public class debugscript// class is public because unity requires the class to be accessible for its internal sysytem to attach it io GameObject
                        // private and protected access specifier not allowed, we can make nested class where nestec class can be provate,protected,public
                        // abstrace class is like a blue print to create other class that share common functionality and abstract class cannot create object directly instead other class inherit form it and must fill in the missing details like(method or properties)
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
         }
}
